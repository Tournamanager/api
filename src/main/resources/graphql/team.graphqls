type Team {
    id: ID!,
    name: String
}

type Query {
    teams(count: Int):[Team]
    team(id: ID!):Team
}

type Mutation {
    createTeam(name:String!):Team
    deleteTeam(id:Int!):String
    addUserToTeam(id:Int!):String
}
